$schema: "http://json-schema.org/draft-07/schema#"

description: aeDNA Trimming configuration file

type: object
properties:

  samples:
    type: string

  units:
    type: string

  reads:
    type: object
    properties:
      extension:
        type: object
        properties:
          activate:
            type: boolean
          k:
            type: integer
          params:
            type: string
        required:
          - activate
          - k
          - params

      derep:
        type: object
        properties:
          tool:
            type: string
            enum: ["vsearch", "seqkit"]
          params:
            type: string
        required:
          - tool
          - params

      low_complex:
        type: object
        properties:
          params:
            type: string
        required:
          - params

    required:
      - extension
      - derep
      - low_complex

  report:
    type: object
    properties:
      multiqc:
        type: string
    required:
      - multiqc

required:
  - samples
  - units
  - reads
  - report
